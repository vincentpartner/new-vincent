# Direkten Browser-Zugriff auf Konfiguration und Datenablage verbieten.
# (PHP kann sie weiterhin einlesen – das ist Datei-Ebene.)
<FilesMatch "^(config\.php|.*\.json|.*\.tmp)$">
  Require all denied
</FilesMatch>
<IfModule mod_autoindex.c>
  Options -Indexes
</IfModule>
